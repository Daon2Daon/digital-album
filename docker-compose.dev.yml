version: '3.8'

# Digital Album - Local Development
# SQLite database (no container needed, app runs on host)
# SQLite file will be created at ./prisma/dev.db

# Note: This file is kept for compatibility but no services are needed
# The app will use SQLite database file directly from the host filesystem
services:
  # No services needed - SQLite runs in-process with the app



